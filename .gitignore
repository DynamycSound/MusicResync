*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin

# MusicResync: local test music corpus (large, not part of source)
/snaptube/


# Disposable self-signed key used to sign the sideloaded release APK (NOT a Play Store key) — committed on
# purpose so CI can produce stable, in-place-upgradeable releases with no secrets. Safe to rotate.
# release-keystore.jks  (intentionally tracked)
# Built release APKs
*.apk
